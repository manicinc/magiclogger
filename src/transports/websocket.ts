/**
 * WebSocket Transport Entry Point
 * 
 * This module provides WebSocket logging transport for MagicLogger.
 * Import this module directly for optimal tree-shaking.
 * 
 * @module transports/websocket
 */

export { WebSocketTransport } from './base/implementations/WebSocketTransport';
export type { WebSocketTransportOptions } from './base/implementations/WebSocketTransport';
